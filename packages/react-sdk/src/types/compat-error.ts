/**
 * Re-export WebBLEError from core -- this file exists only for backward
 * compatibility with imports that referenced the old compat-error path.
 */
export { WebBLEError } from '@ios-web-bluetooth/core';
